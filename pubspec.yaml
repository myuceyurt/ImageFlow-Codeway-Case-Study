name: image_flow
description: An image processing app case study.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  camera: ^0.11.3
  flutter:
    sdk: flutter
  
  get: ^4.6.6

  google_mlkit_face_detection: ^0.13.0
  google_mlkit_text_recognition: ^0.15.0

  hive: ^2.2.3
  hive_flutter: ^1.1.0

  image: ^4.3.0
  image_picker: ^1.1.2
  intl: ^0.20.2
  path_provider: ^2.1.2
  pdf: ^3.11.1
  permission_handler: ^11.3.1
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  very_good_analysis: ^6.0.0

flutter:
  uses-material-design: true
